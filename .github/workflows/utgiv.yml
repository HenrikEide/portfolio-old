name: Deploy Site

on:
  push:
    branches: [master]

  workflow_dispatch:

jobs:
  deploy:
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
      - run: npm install
      - run: npm run deploy
    